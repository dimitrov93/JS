function solve(arr) {
    let proba = arr[4].split(" ");
    proba = proba[1].split("-");
    proba = proba[0];
    console.log(proba);
}

solve(['CS WoW Diablo',
'Install LoL',
'Uninstall WoW',
'Update Diablo',
'Expansion CS-Go',
'Play!'])