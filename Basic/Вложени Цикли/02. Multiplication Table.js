function multiplicTable() {
    for (let one = 1; one <=10 ; one++) {
        for (let two = 1; two <= 10; two++) {
            console.log(`${one} * ${two} = ${one * two}`);
        }
    }
}

multiplicTable()